// tailwind.config.js
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        // 🟢 조선호텔 그린 계열
        josun: {
          dark: "#0F3D2E",     // 메인 딥 그린 (헤더, 강조)
          DEFAULT: "#164A3A",  // 기본 그린
          light: "#1E5C47",   // ← 추가
          soft: "#E6F1EC",     // 연한 배경용
        },

        // 🟡 골드 계열 (외부 일정, 포인트)
        gold: {
          dark: "#8C6A2F",     // 진한 골드 (라인/텍스트)
          DEFAULT: "#C9A24D",  // 기본 골드
          soft: "#F6EFD9",     // 연한 배경
        },

        // 🖨 인쇄용 뉴트럴
        print: {
          line: "#D1D5DB",     // 얇은 테이블 선
          text: "#1F2937",     // 본문 텍스트
          muted: "#6B7280",    // 보조 텍스트
        },
      },

      // 📐 인쇄용 간격 (세로 압축에 도움)
      spacing: {
        "1.5": "0.375rem",
        "2.5": "0.625rem",
      },

      // 🔤 인쇄용 폰트 사이즈
      fontSize: {
        xxs: ["0.65rem", { lineHeight: "0.9rem" }],
      },

      // 🧱 둥글기 최소화 (고급스러운 느낌)
      borderRadius: {
        sm: "0.25rem",
      },
    },
  },
  plugins: [],
};
